import assert from 'assert'
import { Given, When, Then } from '@cucumber/cucumber'

Given('que a matrícula tenha sido cadastrada', function () {
  return 'pending'
})

When('consultar a matrícula', function () {
  return 'pending'
})

Then('os valores das parcelas das faturas devem ser', function () {
  assert(1, 1)
  return 'pending'
})
